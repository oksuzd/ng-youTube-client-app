import { FilterByCriteriaPipe } from './filter-by-criteria.pipe';

describe('FilterByCriteriaPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByCriteriaPipe();
    expect(pipe).toBeTruthy();
  });
});
