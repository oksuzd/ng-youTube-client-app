import { FilterByPopularPipe } from './filter-by-popular.pipe';

describe('FilterByPopularPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByPopularPipe();
    expect(pipe).toBeTruthy();
  });
});
